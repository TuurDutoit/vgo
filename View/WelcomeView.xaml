<UserControl x:Class="View.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <TextBlock>Welcome</TextBlock>
        <TextBox Text="{Binding cName1.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <TextBox Text="{Binding cName2.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding cWidth.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="50" />
            <TextBlock>x</TextBlock>
            <TextBox Text="{Binding cHeight.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="50" />
        </StackPanel>
        <Button Command="{Binding GoToScreen}">Start</Button>
    </StackPanel>
</UserControl>
