<UserControl x:Class="View.GameOverView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <TextBlock Text="Game Over!" FontSize="35" FontWeight="Bold" HorizontalAlignment="Center" />
        <TextBlock Text="{Binding Info}" FontSize="20" HorizontalAlignment="Center" Margin="0,10,0,20" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Command="{Binding CloseCommand}" Padding="5,2,5,2" Margin="0,0,10,0">Close</Button>
            <Button Command="{Binding StartOverCommand}" Padding="5,2,5,2">Start over</Button>
        </StackPanel>
    </StackPanel>
</UserControl>
